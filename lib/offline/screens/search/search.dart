import 'package:flutter/material.dart';

class SearchGuestGuest extends StatelessWidget {
  const SearchGuestGuest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('SearchGuest'),
      ),
      body: Center(
        child: Text('SearchGuest'),
      ),
    );
  }
}
