import 'package:flutter/material.dart';

class CartStore extends StatelessWidget {
  const CartStore({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cart Store'),
      ),
      body: Center(
        child: Text('Cart Store'),
      ),
    );
  }
}
